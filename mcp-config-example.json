{
  "mcpServers": {
    "hub-stdio": {
      "comment": "Local stdio transport - lowest latency, single client",
      "command": "node",
      "args": [
        "/home/user/atlantis/src/infrastructure/server/dist/server.js"
      ],
      "env": {
        "TRANSPORT": "stdio"
      }
    },
    "hub-http": {
      "comment": "HTTP/SSE transport - works with remote hub instances",
      "transport": "sse",
      "url": "http://localhost:8000/sse"
    }
  }
}
