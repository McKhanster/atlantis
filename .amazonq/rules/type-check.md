Before creating a new type, check if the type exists in src/types/mcp.ts.

If it does not exist, after creation you MUST add it to src/types/mcp.ts. 

Always run "npm run type-check" after coding a file. Fix all errors before proceeding to the next file. 